export const defineSecondaryStats = () => {
    return {
        phy: 8 + Math.floor(Math.random() * 10), // between 8 and 18
        int: 8 + Math.floor(Math.random() * 10), // between 8 and 18
        dxt: 8 + Math.floor(Math.random() * 10), // between 8 and 18
        men: 8 + Math.floor(Math.random() * 10), // between 8 and 18
        cha: 8 + Math.floor(Math.random() * 10), // between 8 and 18
    }
}